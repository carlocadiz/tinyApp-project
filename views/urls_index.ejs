<!DOCTYPE html>
<html lang="en">
<head>
  <title>tinyAPP</title>
</head>

<header>
    <% include ./partials/_header %>
</header>

<body>


      <% for (var url in urls) { %>
      <p></p>
       <center><span><a href= <%=`${urls[url].long}`%>><%=` ${url} `%></a><%=`----- (${urls[url].long}) ` %></center></span>
<% if(user){ %>
    <form action="/urls/<%= url %>">
     <center> <button type="submit">EDIT</button></center></form>

     <form method="POST" action="/urls/<%=url%>/delete">
    <center> <button type="submit">DELETE </button></</center></form>
<% }; %>
 <% } %>
<!--<p><a href="#">SHORTEN NEW URL</a></p>-->
<P>&nbsp;<P>
<form method="GET" action= "/urls/new" >
<center><button type="submit" >SHORTEN NEW URL</button></form></center>
</body>
</html>